# AutoShareQuests

Automatically share all quests when someone joins your group

commands:

`/asq` - shares all quests manually
`/asq auto` - toggles auto-share on player join
`/asq silent` - toggles messages in chat
